services:
  measurement-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: measurement-app
    restart: always
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
